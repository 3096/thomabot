{
  "name": "thomabot",
  "version": "0.0.1",
  "description": "housekeeper of the house",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -w",
    "test": "nodemon -w ./out --experimental-specifier-resolution=node -r source-map-support/register ./out/index.js",
    "deploy": "node --experimental-specifier-resolution=node -r source-map-support/register ./out/deploy-commands.js"
  },
  "author": "3096",
  "license": "UNLICENSED",
  "dependencies": {
    "@discordjs/builders": "^0.11.0",
    "@discordjs/rest": "^0.2.0-canary.0",
    "discord-api-types": "^0.26.1",
    "discord.js": "^13.5.0",
    "dotenv": "^10.0.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "eslint": "^8.5.0",
    "nodemon": "^2.0.15"
  }
}
